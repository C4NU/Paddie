name: Build Paddie

on:
  push:
    branches: [ "main" ]
    tags:
      - "v*"
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r src/requirements.txt
        pip install pyinstaller

    - name: Install Linux dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y libxcb-xinerama0 libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-render-util0 libxcb-shape0 libxkbcommon-x11-0

    - name: Build with PyInstaller
      run: |
        pyinstaller --noconfirm Paddie.spec

    - name: Upload Artifact (Windows)
      if: matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: Paddie-Windows
        path: dist/Paddie

    - name: Upload Artifact (macOS)
      if: matrix.os == 'macos-latest'
      uses: actions/upload-artifact@v4
      with:
        name: Paddie-macOS
        path: dist/Paddie.app

    - name: Upload Artifact (Linux)
      if: matrix.os == 'ubuntu-latest'
      uses: actions/upload-artifact@v4
      with:
        name: Paddie-Linux
        path: dist/Paddie

  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Prepare Assets
      run: |
        mkdir -p dist
        # Windows: Zip the executable (it's often a single file or a directory depending on spec)
        cd artifacts/Paddie-Windows && zip -r ../../dist/Paddie-Windows.zip . && cd ../..
        # macOS: Zip the .app bundle
        cd artifacts/Paddie-macOS && zip -r ../../dist/Paddie-macOS-app.zip . && cd ../..
        # Linux: Zip the executable
        cd artifacts/Paddie-Linux && zip -r ../../dist/Paddie-Linux.zip . && cd ../..

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/Paddie-Windows.zip
          dist/Paddie-macOS-app.zip
          dist/Paddie-Linux.zip
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
